# pnpm configuration — applies to all environments
# NOTE: Do NOT add store-dir here. That is a machine-local setting
# and will break CI if committed. Configure it in ~/.npmrc locally.

shamefully-hoist=false
strict-peer-dependencies=false
